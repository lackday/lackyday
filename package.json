{
  "name": "global-draw-lottery",
  "version": "1.0.0",
  "private": true,
  "description": "Daily virtual-coin lottery — real deployment build (Node/Express backend + vanilla JS frontend).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "node-cron": "^3.0.3"
  }
}
